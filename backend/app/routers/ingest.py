"""Ingestion endpoints for building the RAG index. Skeleton only."""

from fastapi import APIRouter

router = APIRouter(prefix="/ingest", tags=["ingest"])

# TODO: endpoints to build/refresh Chroma collection from the twcs dataset.
