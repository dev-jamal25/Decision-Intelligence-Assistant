"""POST /analyze — main orchestration endpoint. Skeleton only."""

from fastapi import APIRouter

router = APIRouter(prefix="/analyze", tags=["analyze"])

# TODO: POST / — accept AnalyzeRequest, dispatch to analyze_service, return AnalyzeResponse.
